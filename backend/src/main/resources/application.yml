server:
  port: 8080
spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/networkstream}
    username: ${DB_USER:networkstream}
    password: ${DB_PASSWORD:networkstream}
  sql:
    init:
      mode: always
management:
  endpoints:
    web:
      exposure:
        include: health,info
